///
/// HybridSportscarSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#include <NitroModules/JHybridObject.hpp>
#include <fbjni/fbjni.h>
#include "HybridSportscarSpec.hpp"




namespace margelo::nitro::sportscar {

  using namespace facebook;

  class JHybridSportscarSpec: public jni::HybridClass<JHybridSportscarSpec, JHybridObject>,
                              public virtual HybridSportscarSpec {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/sportscar/HybridSportscarSpec;";
    static jni::local_ref<jhybriddata> initHybrid(jni::alias_ref<jhybridobject> jThis);
    static void registerNatives();

  protected:
    // C++ constructor (called from Java via `initHybrid()`)
    explicit JHybridSportscarSpec(jni::alias_ref<jhybridobject> jThis) :
      HybridObject(HybridSportscarSpec::TAG),
      HybridBase(jThis),
      _javaPart(jni::make_global(jThis)) {}

  public:
    ~JHybridSportscarSpec() override {
      // Hermes GC can destroy JS objects on a non-JNI Thread.
      jni::ThreadScope::WithClassLoader([&] { _javaPart.reset(); });
    }

  public:
    size_t getExternalMemorySize() noexcept override;
    void dispose() noexcept override;

  public:
    inline const jni::global_ref<JHybridSportscarSpec::javaobject>& getJavaPart() const noexcept {
      return _javaPart;
    }

  public:
    // Properties
    

  public:
    // Methods
    std::shared_ptr<Promise<bool>> initializeMediaLibrary(const std::string& mediaLibraryJson) override;
    std::shared_ptr<Promise<bool>> updateMediaLibrary(const std::string& mediaLibraryJson) override;
    std::shared_ptr<Promise<std::string>> getMediaLibrary() override;
    std::shared_ptr<Promise<bool>> setLayoutType(const std::string& layoutType) override;
    std::shared_ptr<Promise<bool>> refreshAndroidAutoUI() override;
    std::shared_ptr<Promise<bool>> playMedia(const std::string& mediaId) override;
    std::shared_ptr<Promise<bool>> pause() override;
    std::shared_ptr<Promise<bool>> resume() override;
    std::shared_ptr<Promise<bool>> stop() override;
    std::shared_ptr<Promise<bool>> seekTo(double positionMs) override;
    std::shared_ptr<Promise<bool>> setPlaybackSpeed(double speed) override;
    std::shared_ptr<Promise<bool>> skipToNext() override;
    std::shared_ptr<Promise<bool>> skipToPrevious() override;
    std::shared_ptr<Promise<PlaybackInfo>> getPlaybackState() override;
    std::shared_ptr<Promise<bool>> isCurrentlyPlaying() override;
    std::shared_ptr<Promise<std::optional<LastPlayedMediaInfo>>> getLastPlayedMediaInfo() override;
    std::shared_ptr<Promise<bool>> handleAppStateChange(const std::string& appState) override;
    std::shared_ptr<Promise<AppState>> getCurrentAppState() override;

  private:
    friend HybridBase;
    using HybridBase::HybridBase;
    jni::global_ref<JHybridSportscarSpec::javaobject> _javaPart;
  };

} // namespace margelo::nitro::sportscar
