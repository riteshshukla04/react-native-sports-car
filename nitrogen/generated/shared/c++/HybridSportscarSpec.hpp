///
/// HybridSportscarSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `PlaybackInfo` to properly resolve imports.
namespace margelo::nitro::sportscar { struct PlaybackInfo; }
// Forward declaration of `LastPlayedMediaInfo` to properly resolve imports.
namespace margelo::nitro::sportscar { struct LastPlayedMediaInfo; }
// Forward declaration of `AppState` to properly resolve imports.
namespace margelo::nitro::sportscar { enum class AppState; }

#include <NitroModules/Promise.hpp>
#include <string>
#include "PlaybackInfo.hpp"
#include "LastPlayedMediaInfo.hpp"
#include <optional>
#include "AppState.hpp"

namespace margelo::nitro::sportscar {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Sportscar`
   * Inherit this class to create instances of `HybridSportscarSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSportscar: public HybridSportscarSpec {
   * public:
   *   HybridSportscar(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSportscarSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSportscarSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSportscarSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> initializeMediaLibrary(const std::string& mediaLibraryJson) = 0;
      virtual std::shared_ptr<Promise<bool>> updateMediaLibrary(const std::string& mediaLibraryJson) = 0;
      virtual std::shared_ptr<Promise<std::string>> getMediaLibrary() = 0;
      virtual std::shared_ptr<Promise<bool>> setLayoutType(const std::string& layoutType) = 0;
      virtual std::shared_ptr<Promise<bool>> refreshAndroidAutoUI() = 0;
      virtual std::shared_ptr<Promise<bool>> playMedia(const std::string& mediaId) = 0;
      virtual std::shared_ptr<Promise<bool>> pause() = 0;
      virtual std::shared_ptr<Promise<bool>> resume() = 0;
      virtual std::shared_ptr<Promise<bool>> stop() = 0;
      virtual std::shared_ptr<Promise<bool>> seekTo(double positionMs) = 0;
      virtual std::shared_ptr<Promise<bool>> setPlaybackSpeed(double speed) = 0;
      virtual std::shared_ptr<Promise<PlaybackInfo>> getPlaybackState() = 0;
      virtual std::shared_ptr<Promise<bool>> isCurrentlyPlaying() = 0;
      virtual std::shared_ptr<Promise<std::optional<LastPlayedMediaInfo>>> getLastPlayedMediaInfo() = 0;
      virtual std::shared_ptr<Promise<bool>> handleAppStateChange(const std::string& appState) = 0;
      virtual std::shared_ptr<Promise<AppState>> getCurrentAppState() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Sportscar";
  };

} // namespace margelo::nitro::sportscar
